services:
  - type: web
    name: youtube-to-playlist
    env: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
    staticPublishPath: build